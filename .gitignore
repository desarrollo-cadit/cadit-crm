# --- Secretos (NUNCA al control de versiones) ---
.env
.env.*
!.env.example
*.pem
*.key

# --- MCP local con tokens (si lo personalizas con secretos en claro) ---
# .mcp.json se commitea SOLO si usa ${VAR} (sin tokens en claro). Si metes
# secretos directos, ignóralo y deja únicamente .mcp.json.example.
# .mcp.json

# --- Dependencias / build (cuando el starter ya tenga app encima) ---
node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo

# --- Logs y temporales ---
*.log
npm-debug.log*
pnpm-debug.log*
.cache/
tmp/
.tmp/

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# --- Claude Code: estado local (no compartir) ---
# La memoria de subagentes (.claude/agent-memory/) SÍ se versiona (es memoria de
# proyecto compartida con el equipo). La config local de la sesión, no:
.claude/settings.local.json
.playwright-mcp/
.tmp-seed-demo.mjs
.tmp-seed-academic.mjs
.tmp-import-cadit.mjs
.tmp-verify-billing.mjs
.tmp-verify-grading.mjs
.tmp-reset-password.mjs
.tmp-dar-acceso.mjs
dev.log
.dev-media/

# --- Self-test E2E contra base efímera (ver scripts/e2e-selftest.mjs) ---
# El arnés REGISTRA un operador, así que corre contra su propia base y su
# propio dist dir: dos procesos de Next sobre el mismo `.next` se corrompen
# el grafo de módulos.
e2e-dev.log
dev-e2e.log
.next-e2e/
.dev-media-e2e/

# --- Skill registry index (regenerable) ---
.atl/

# --- CodeGraph / Gentle AI: estado local de la herramienta (no compartir) ---
.codegraph/
.gga
.tmp-verificar-aulas.mjs
.tmp-demo-camada.mjs

# Contraseña suelta de una prueba local: nunca al repositorio.
.tmp-pass.txt
